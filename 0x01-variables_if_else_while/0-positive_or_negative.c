#include <stdio.h>
/**
* main - Prints the alphabet in lowercase.
*
* Return: Always 0.
*/
int main(void)
{
int a=0;
int z=0;
int n=0;
char letter;
for (letter = a; letter <= z; letter++)
putchar(letter);
putchar(n);
return (0);
}
