#include <stdio.h>
/**
*main - print lowercase a-z
*Return: Always 0 (Success)
*/
int main(void)
{
int a = 0;
int z = 0;
int n = 0;
char alpha =a;
while (alpha <= z)
{
putchar(alpha);
alpha++;
}
putchar(n);
return (0);
}
