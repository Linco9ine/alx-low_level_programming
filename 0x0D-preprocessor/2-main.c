#include <stdio.h>

/**
 * main -main entry
 * Description: Program that prints the name of the file
 * it was compiled from
 * Return: 0
 */
int main(void)
{
	_putchar("%s\n", __FILE__);

	return (0);
}
